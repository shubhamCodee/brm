1) model events ing=>[before events] ed=>[after events]. (created/updated)specific (saved)general
2) debugbar => n+1 problem
3) failed_jobs queue:work; queue:retry <uuid>; queue:forget <uuid>; custom command
4) redis => in memory-store; key-value data store
5) create/edit form optimization => reusable components
6) massUpdate and massDelete => Service(specialist)

netstat -ano | findstr :5173
taskkill /F /PID 9392

optimization code. 
passport
apis
pagination
